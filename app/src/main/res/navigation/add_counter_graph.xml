<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/add_counter_graph"
    app:startDestination="@id/add_counter_fragment">

    <fragment
        android:id="@+id/add_counter_fragment"
        android:name="com.cornershop.counterstest.presentation.counters.addcounter.AddCounterFragment"
        android:label="@string/create_counter"
        tools:layout="@layout/fragment_add_counter" />

    <fragment
        android:id="@+id/select_counter_fragment"
        android:name="com.cornershop.counterstest.presentation.counters.selectcounter.SelectCounterFragment"
        android:label="@string/examples"
        tools:layout="@layout/fragment_select_counter" />

</navigation>